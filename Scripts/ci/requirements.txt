# Direct gate dependencies; transitive packages are recorded by pip freeze.
# Keep compatible with the shared docs requirements, without changing them.
-r ../../docs/requirements.txt
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-minify-plugin==0.8.0
pymdown-extensions==11.0.1
PyYAML==6.0.2
