Read and obey `docs/development/prime-agent-run/goal.md` and `operating-contract.md` as durable programme authority.

Start now. Do not ask me to choose the next task. Verify the environment, discover the Linear MCP tools, fetch the entire zephyr-flow project and all full issue bodies, populate the ledger, create the required internal heartbeats, record baseline checks, build the dependency graph and execute every dependency-ready autonomous task. Use isolated worktrees and persistent RLM children for parallel work, with no more than three concurrent code writers. Integrate and push accepted work continuously.

For human, credential, production-service, independent-review and unavailable real-device gates, complete every preparatory artifact and leave an exact non-Done disposition; continue all independent work instead of waiting. Apply only conservative reversible provisional assumptions and record them.

Do not stop after planning. Continue through implementation, testing, integration, Linear evidence updates, release/eval harness preparation and final gate repair until the persistent goal is honestly complete or an external host budget prevents another continuation.
